diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js
index 1234567..abcdefg 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js
@@ -1306,7 +1306,7 @@ const makeMessagesRecvSocket = (config) => {
                         const unavailableType = unavailableNode?.attrs?.type;
                         if (unavailableType === 'bot_unavailable_fanout' ||
-                            unavailableType === 'view_once_unavailable_fanout' ||
                             unavailableType === 'hosted_unavailable_fanout') {
+                            // NOTE: view_once_unavailable_fanout intentionally NOT skipped
+                            // so the bot can request placeholder resend to get view-once content
                             logger.debug({ msgId: msg.key.id, unavailableType }, 'skipping placeholder resend for excluded unavailable type');
                             acked = true;
                             return sendMessageAck(node);
